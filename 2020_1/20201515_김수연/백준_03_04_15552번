//for문_빠른 A+B
#include <iostream>
using namespace std;

int main(void) {
	int a, b, c;
	cin.tie(NULL);
	ios::sync_with_stdio(false);

	cin >> a;

	for (int i = 0; i < a; i++) {
		cin >> b >> c;
		cout << b + c << "\n";
	}

	return 0;
}
