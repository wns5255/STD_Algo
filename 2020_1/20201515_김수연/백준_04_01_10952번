//while문_A+B-5
#include <iostream>
using namespace std;

int main(void) {
	int a, b;
	cin.tie(NULL);
	ios::sync_with_stdio(false);

	cin >> a >> b;
	while (a != 0 && b != 0) {
		cout << a + b << endl;
		cin >> a >> b;
	}
	
	return 0;
}
