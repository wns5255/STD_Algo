//if문_알람시계
#include <iostream>
using namespace std;

int main(void) {
	int a, b;
	cin >> a >> b;

	if (b < 45 && a >= 0) {
		if (a == 0)
			a = 24;
		a--;
		b += 15;
	}
	else
		b -= 45;

	cout << a << ' ' << b << endl;
	
	return 0;
}
