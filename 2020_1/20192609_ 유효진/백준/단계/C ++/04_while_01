#include <iostream>
#include <ios>
using namespace std;

int main()
{
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	
	int a, b;

	while (1)
	{
		cin >> a >> b;
		if (a != 0 && b != 0)
			cout << a + b << "\n";
		else
			break;
	}

	return 0;
}
