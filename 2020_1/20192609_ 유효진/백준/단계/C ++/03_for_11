#include <iostream>
#include <ios>
using namespace std;

int main()
{
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	
	int n, x, a;
	cin >> n >> x;
	int* p = new int[n];

	for (int i =0; i<n; i++)
	{
		cin >> a;
		p[i] = a;
	}

	for (int i = 0; i < n; i++)
	{
		if (p[i] < x)
			cout << p[i] << " ";
	}

	return 0;
}
