/* 문제 2
앞서 소개한 예제 RefSwap.cpp의 SwapByRef2 함수를 다음의 형태로 호출하면 컴파일 에러가 발생한다.
SwapByRef2(23, 45);

컴파일 에러가 발생하는 이유가 무엇인지 설명해보자.

-> 매개변수의 선언 위치에 변수가 아닌 상수가 선언되었기 때문.
그런데 참조자는 상수를 참조할 수 없기 때문에 컴파일 에러가 발생함.*/
